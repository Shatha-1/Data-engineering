# Streaming
kafka-python-ng==2.2.3
pydantic==2.9.2

# Lakehouse
# NOTE: requires Java 17 (Spark 4.x dropped Java 8/11 support)
pyspark==4.1.0
delta-spark==4.1.0

# Orchestration
# NOTE: Airflow 3.x changed several internals vs 2.x, but this project's
# dags/pipeline_dag.py already uses the TaskFlow API (@dag/@task), which
# carries over with no code changes needed.
apache-airflow==3.2.0

# Data quality
great_expectations==1.3.7

# Lineage
openlineage-python==1.28.0

# RAG
chromadb==1.4.1
sentence-transformers==5.4.1
rank-bm25==0.2.2
groq==0.13.0

# Data acquisition / utility
pandas==2.2.3
numpy==2.1.3
python-dotenv==1.0.1
